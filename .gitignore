server/.env
server/venv/
server/storage/
sessions/
__pycache__/
*.py[cod]
web/public/imagery/
contoh-orthommosaic-drone/
docs/soerogis_frontend-main/
soerogis_frontend/
misi_jember.waypoints
# Secrets / TLS certs (regenerated by ensure-cert.sh — never commit private keys)
certs/

# Project-local Postgres runtime state (binary cluster data + sockets)
pgdata/
pgrun/

# Python virtualenvs, caches, logs
webview_venv/
.cache/
.pytest_cache/
*.log

# Large data & model artifacts — kept out of git (store externally / regenerate).
# ndvi/ holds a 173M zip that exceeds GitHub's 100MB per-file limit.
datasets/
ndvi/
downloads/
cv_model/*.pt
.claude

*.mp4
